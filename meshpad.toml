[meshpad]
name        = "meshpad-starter"
version     = "1.0.0"
description = "Starter meshpad — fork and customize for your stack"

[meshpad.mesh]
min_version = "0.5.0"
steermesh   = "../mesh/steermesh.toml"

[meshpad.distribution]
type   = "git"
remote = "github.com/SteerMesh/meshpad"
branch = "main"
